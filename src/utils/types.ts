export interface ICharacter {
  name: string;
  status: string;
  gender: string;
  originName: string;
  image: string;
}

export interface ICharacterResInfo {
  count: number;
  pages: number;
  next: string;
  prev: number;
}
